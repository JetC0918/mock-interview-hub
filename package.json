{
    "name": "mock-interview-hub",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"make run -C backend\"",
        "install:all": "npm install && npm install --prefix frontend && make install -C backend"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}